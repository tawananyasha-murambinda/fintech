name: Build APK & IPA

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  apk:
    name: Build APK (Android)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - uses: actions/setup-java@v4
        with:
          distribution: 'microsoft'
          java-version: '21'

      - name: Install dependencies
        run: npm ci

      - name: Build Next.js
        run: npm run build

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Build APK
        run: cd android && ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: FinTrack-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk

  ipa:
    name: Build IPA (iOS)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build Next.js
        run: npm run build

      - name: Add iOS platform
        run: npx cap add ios

      - name: Sync Capacitor
        run: npx cap sync ios

      - name: Install CocoaPods
        run: cd ios && pod install

      - name: Build IPA
        run: |
          cd ios
          xcodebuild -workspace App.xcworkspace -scheme App -configuration Debug -sdk iphoneos -allowProvisioningUpdates build CODE_SIGNING_ALLOWED=NO || true
          mkdir -p build
          xcodebuild -workspace App.xcworkspace -scheme App -configuration Debug -sdk iphoneos -allowProvisioningUpdates archive -archivePath build/FinTrack.xcarchive CODE_SIGNING_ALLOWED=NO || true

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: FinTrack-iOS-Build
          path: ios/build/

      - name: Upload .app
        uses: actions/upload-artifact@v4
        with:
          name: FinTrack-app
          path: ios/build/FinTrack.xcarchive/Products/Applications/
